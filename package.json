{
  "name": "korean-word-lookup",
  "version": "2.5.1",
  "private": true,
  "type": "module",
  "description": "Validation tooling for the Korean Word Lookup Chrome extension",
  "scripts": {
    "validate": "node tools/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
